<%- include("./partials/nav.ejs") %>
<title>Home</title>

<main>
    <h1>Authentication App</h1>

    <% if (user) { %>
        <p>Welcome, <%= user.username %>.</p>
    <% } else { %>
        <p>Your Express, MongoDB, Mongoose, EJS, and session setup is ready.</p>
    <% } %>
</main>
</body>
</html>
